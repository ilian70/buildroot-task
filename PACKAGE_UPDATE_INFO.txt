BUILDROOT PACKAGE UPDATE - Source Code Changes
================================================

Your redis-image-viewer package in buildroot is already configured to use your local source code.

PACKAGE CONFIGURATION:
---------------------
Location: /home/innocore/Projects/br-task/buildroot-package/redis-image-viewer/

Key Configuration Files:
1. redis-image-viewer.mk (Makefile recipe)
   - Points to local source: REDIS_IMAGE_VIEWER_SITE = $(TOPDIR)/../br-task
   - Uses CMake build system
   - Includes all dependencies: sdl2, sdl2_image, hiredis
   - Installs systemd service for auto-start

2. Config.in (Buildroot configuration)
   - Defines package options visible in menuconfig

3. redis-image-viewer.service (Systemd service file)
   - Ensures the app runs as a systemd service

HOW IT WORKS:
-----------
The buildroot package uses REDIS_IMAGE_VIEWER_SITE_METHOD = local
This means:
- Source code is taken directly from /home/innocore/Projects/br-task/
- Any changes you make to your source files are automatically included in the next build
- No need to manually copy files or update checksums

YOUR SOURCE FILES:
------------------
Your application compiles these files:
- main.cpp
- app.cpp / app.h
- sdl_ctx.cpp / sdl_ctx.h
- redis_conn.cpp / redis_conn.h
- app_cfg.cpp
- json11.cpp
- fbdraw.cpp (framebuffer utilities)
- Plus configuration files (app.cfg.json)

REBUILDING WITH CHANGES:
------------------------
To rebuild your package with the latest source changes:

1. Full rebuild (recommended after code changes):
   make -C /home/innocore/Projects/buildroot redis-image-viewer-rebuild

2. Or complete image rebuild:
   make -C /home/innocore/Projects/buildroot

3. To clean and rebuild from scratch:
   make -C /home/innocore/Projects/buildroot redis-image-viewer-clean
   make -C /home/innocore/Projects/buildroot

WHAT'S ALREADY SET UP:
--------------------
✅ Local source directory configured
✅ CMake build system
✅ All dependencies declared (SDL2, Hiredis, etc.)
✅ SSH key registration (via post-build.sh)
✅ Systemd service auto-start
✅ Config file installation (/etc/redis-image-viewer/config.json)
✅ Image directory (/var/lib/redis-image-viewer/images)

NO FURTHER ACTION NEEDED - Your package is ready to rebuild with any source changes!
